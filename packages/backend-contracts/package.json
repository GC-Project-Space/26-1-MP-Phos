{
  "name": "@phos/backend-contracts",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tspc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@phos/shared": "workspace:*",
    "typia": "^12.0.1"
  },
  "devDependencies": {
    "ts-patch": "^3.3.0",
    "typescript": "^6.0.2"
  }
}
