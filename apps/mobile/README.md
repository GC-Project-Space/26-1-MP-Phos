# Phos Mobile

Flutter 모바일 앱입니다. React Native/Expo 앱을 대체하며 `apps/mobile` 경로를 승계합니다.

## Getting Started

```bash
flutter pub get
flutter run
```

## Verification

```bash
flutter analyze
flutter test
```

루트에서는 `pnpm dev:mobile`, `pnpm mobile:analyze`, `pnpm mobile:test`로 같은 작업을 실행할 수 있습니다.
